name: ivas_app
description: "IVAS — Intelligent Viva Assessment System Student App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.2.1
  web_socket_channel: ^3.0.3
  speech_to_text: ^7.0.0
  record: ^6.0.0
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  audioplayers: ^6.1.0
  path_provider: ^2.1.5
  dart_mappable: ^4.3.0
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

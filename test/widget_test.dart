import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads without error', (WidgetTester tester) async {
    // Placeholder test — voice/network features require integration testing
    expect(1 + 1, equals(2));
  });
}
